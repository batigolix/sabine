{% set classes = [
  'page-title',
] %}
<section id="top" {{ attributes.addClass(classes) }}>
  <div class="container">
      {{ title_prefix }}
      {% if title %}
        <h1{{ title_attributes }}>{{ title }}</h1>
      {% endif %}
      {{ title_suffix }}
  </div>
</section>
